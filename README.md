# Telegram bot on Flask

Simple starter project for a Telegram bot deployed on Railway.

## Run locally

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

## Environment

Set these variables:

```bash
BOT_TOKEN=your_token_here
WEBHOOK_PATH=/telegram
```
